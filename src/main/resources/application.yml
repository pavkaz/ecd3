spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/graduate_work
    username: root
    password: root

  servlet:
    multipart:
      max-file-size: 300MB
      max-request-size: 3000MB

  freemarker:
    expose-request-attributes: true

  test:
    database:
      replace: none